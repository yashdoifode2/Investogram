from django.apps import AppConfig

class EmailForensicsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'email_forensics'
    verbose_name = 'Email Forensics & OSINT'